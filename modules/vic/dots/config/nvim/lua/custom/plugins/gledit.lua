return {
  {
    'https://gitlab.com/jarv/gledit.git',
    config = function()
      -- Optional: Set GitLab token (alternatively use GITLAB_TOKEN env var)
      -- vim.g.gitlab_token = 'your_gitlab_token_here'
    end,
  },
}
